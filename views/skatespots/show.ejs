<% include ../partials/header %>

    <div class="container">
        <div class="row">
            <div class="col-md-3">
                    <p class="lead"> Location</p>
                    <div class="list-group">
                        <li class="list-group-item active">Info 1</li>
                        <li class="list-group-item">Info 2</li>
                        <li class="list-group-item active">Info 3</li>
                    </div>
            </div>
            
            <div class="col-md-9">
                    <div class="thumbnail">
                        <img class="img-responsive" src="<%= skatespot.image %>">
                        <div class="caption-full">
                            <!--<h4 class="pull-right">$/night</h4>-->
                            <h4><a><%= skatespot.name %></a></h4>
                            <p><%= skatespot.description %></p>
                            <p>
                                <em>Submitted By <%= skatespot.author.username %></em>
                            </p>
                        </div>
                    </div>
                    <div class="well">
                        <div class="text-right">
                          <a class="btn btn-success" href="/skateSpots/<%= skatespot._id %>/comments/new">Add a Review</a>  
                        </div>
                        <hr>
                        <% skatespot.comments.forEach(function(comment){ %>
                            <div class="row">
                                <div class="col-md-12">
                                        <strong><%= comment.author.username %></strong>
                                        <p style="display: inline;">
                                          <%= comment.text %>
                                        </p>
                                        <a class="btn btn-xs btn-warning" href="/skateSpots/<%= skatespot._id %>/comments/<%=comment._id %>/edit">Edit</a>
                                        <form id="delete-form"  style="display: inline" action="/skateSpots/<%= skatespot._id %>/comments/<%=comment._id %>?_method=DELETE" method="POST">
                                            <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                                        </form>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
        </div>
    </div>
        
        <a href="/skateSpots">Go Back</a>
        
        <% include ../partials/footer %>